package edu.ntnu.iir.bidata;


public class Main {
  /**
   * Main method.
   */
  public static void main(String[] args) {
    // creates a local variable ui for the class UserInterface.
    UserInterface ui = new UserInterface();

    //Calling the method start() in UserInterface
    ui.start();

    // Calling the method init() in UserInterface
    // ui.init();
  }
}
